# UDS 诊断页面 UI 优化方案

## 目标

当前 UDS 诊断页面功能已经覆盖单次发送、自动化流程、DTC、刷写和日志，但随着功能增多，页面开始偏“功能堆叠”。本方案目标是优化信息层级和操作效率，不改变现有协议行为。

核心原则：

- 优先提升现场使用效率。
- 不改底层 UDS 收发逻辑。
- 保持现有默认行为。
- 危险操作明确区分。
- 功能多但界面不乱。

## 总体 UI 原则

### 1. 工作流分区

每个 Tab 尽量遵循：

```text
参数区
↓
主内容区
↓
操作/状态区
```

### 2. 危险操作突出

以下按钮应使用统一危险样式：

- 清空流程
- 清空日志
- 清除 DTC
- 中止升级
- 中止流程

### 3. 表格优先可读

表格应优先保证关键列可读：

- 请求 PDU
- 响应 PDU
- 期望响应
- 状态

低频列可以收窄或通过下拉/默认值减少编辑成本。

### 4. 容错原则

- UI 调整不能改变已有数据含义。
- 表格单元格缺失时必须兜底。
- 下拉框非法值必须回退默认值。
- 搜索/过滤为空时显示全部。
- 清空类操作必须确认。

## Batch UI-1：自动化流程表格优化

### 目标

提升自动化流程表格可读性和编辑效率。

### 修改内容

- 优化列宽：
  - 选择：40
  - 名称：140
  - 请求 PDU：150
  - 响应 PDU：170
  - 状态：90
  - 耗时：70
  - 期望响应：150
  - 匹配：80
  - 失败策略：90
- 状态列居中显示。
- 状态列使用颜色：
  - 等待：浅灰
  - 发送中/延时中：黄色
  - 成功：绿色
  - 错误/超时/断言失败：红色
- 匹配列默认值：`前缀`
- 失败策略默认值：`继续`
- 新增行时自动填充默认值。

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`

### 执行步骤

1. 调整 `m_flowTable` 列宽。
2. 新增状态单元格格式化辅助函数：
   - `setFlowStepStatus(row, text, color)`
3. 替换现有多处直接设置状态文本/颜色的代码。
4. 新增流程行时统一填充默认列。

### 容错处理

- `row` 越界时直接返回。
- 状态单元格不存在时创建。
- 未识别状态使用默认灰色。

### 验证点

- 添加普通 UDS 步骤。
- 添加延时步骤。
- 执行成功、负响应、超时、断言失败。
- 表格没有因为状态变化产生布局跳动。

## Batch UI-2：流程操作按钮分组

### 目标

降低误操作风险，让编辑、导入导出、执行动作更清晰。

### 修改内容

将流程按钮分为三组：

```text
编辑：添加延时 / 删除 / 清空 / 上移 / 下移
文件：导入流程 / 导出流程
执行：循环次数 / 请求间隔 / 列表发送
```

建议：

- `清空列表` 使用危险样式。
- `列表发送` 独立靠右。
- 运行中只保留中止按钮为醒目状态。

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`

### 执行步骤

1. 拆分现有 `flowActionsLayout`。
2. 新增小标题或使用 spacing 区分按钮组。
3. 保持原有信号连接不变。
4. 清空列表前增加确认框。

### 容错处理

- 流程运行中禁止导入流程。
- 流程运行中禁止清空列表。
- 清空列表时二次确认。

### 验证点

- 所有按钮仍能触发原功能。
- 流程运行中点击中止正常恢复 UI。
- 清空列表确认取消后数据保留。

## Batch UI-3：单次服务调试区优化

### 目标

让单次发送更像一个清晰的“发送台”。

### 修改内容

布局调整为两行：

```text
请求 PDU | 输入框 | 立即发送 | 添加到列表
响应 PDU | 只读框 | 响应状态
```

新增响应状态标签：

- 等待
- 正响应
- 负响应
- 超时
- 发送失败

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`
- `qt-example_32/CAN/udswidget.h`

### 执行步骤

1. 新增 `m_pduStatusLabel`。
2. 发送前设置为 `等待响应`。
3. 正响应、负响应、超时回调中更新状态。
4. 状态标签使用颜色区分。

### 容错处理

- 非自动化响应也能更新状态。
- 发送失败时状态显示失败。
- 手动清空请求不影响响应框。

### 验证点

- 合法请求发送后显示等待。
- 正响应显示绿色。
- 负响应显示红色。
- 超时显示红色。

## Batch UI-4：服务树搜索与快捷操作

### 目标

服务模板变多后，提升查找效率。

### 修改内容

- 服务树上方增加搜索框。
- 输入关键字过滤节点。
- 支持按服务号、名称、PDU 搜索。
- 双击叶子节点后：
  - 自动填入请求 PDU。
  - 自动聚焦请求输入框。
- 可选：右键添加到流程。

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`
- `qt-example_32/CAN/udswidget.h`

### 执行步骤

1. 新增 `m_serviceSearchEdit`。
2. 新增 `filterServiceTree(QString keyword)`。
3. 遍历树节点，匹配则显示，不匹配则隐藏。
4. 父节点在任一子节点匹配时显示。
5. 双击后调用 `m_pduReqEdit->setFocus()`。

### 容错处理

- 搜索框为空时显示全部。
- 搜索不区分大小写。
- 节点缺少 PDU 字段时不报错。

### 验证点

- 搜索 `22`。
- 搜索 `DTC`。
- 搜索不存在内容。
- 清空搜索恢复全部。

## Batch UI-5：日志过滤与搜索

### 目标

提高日志排查效率，尤其是刷写和自动化流程场景。

### 修改内容

新增日志工具栏：

- 类型过滤：
  - 全部
  - TX
  - RX
  - ERR
  - SYS
- 搜索框
- 清空日志
- 导出日志

建议保留原始日志数据列表，显示层做过滤。

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`
- `qt-example_32/CAN/udswidget.h`

### 执行步骤

1. 新增日志缓存结构：
   - 文本
   - 类型
   - 时间
2. `onLogMessage()` 先写缓存，再刷新显示。
3. 新增 `refreshLogView()`。
4. 类型过滤和搜索框变化时刷新显示。

### 容错处理

- 搜索为空显示全部。
- 类型为未知值时按 SYS 处理。
- 日志数量过大时限制缓存上限，例如 5000 行。
- 导出日志默认导出全部还是当前过滤结果，需要在 UI 上明确。

### 验证点

- TX/RX/ERR/SYS 过滤。
- 搜索关键字。
- 清空日志后缓存清空。
- 导出日志内容符合预期。

## Batch UI-6：刷写配置区分组优化

### 目标

降低 Batch A 后刷写配置区的密度，让配置更容易理解。

### 修改内容

将刷写参数分为四组：

```text
基础：固件文件 / 起始地址 / Reset 类型
安全访问：Seed 子功能 / Key 子功能
下载传输：DFI / ALFI / 默认块大小 / 使用 ECU 块大小
校验：Routine ID / 追加 CRC32 / CRC 大小端
```

### 建议涉及文件

- `qt-example_32/CAN/udswidget.cpp`

### 执行步骤

1. 拆分当前刷写配置区域。
2. 保留原控件和默认值。
3. 调整控件排列。
4. 不改 `buildUpgradeConfig()` 逻辑。

### 容错处理

- 只调整布局，不改变控件对象。
- 所有控件仍参与升级期间锁定。
- 默认值保持不变。

### 验证点

- 默认配置仍能启动升级。
- 非法配置仍能被拦截。
- 升级期间新增配置控件全部锁定。

## 简化实施顺序

将 6 个 UI 优化点合并为 3 批执行，减少切换成本。

### 第一批：诊断操作区优化

合并内容：

- Batch UI-1：自动化流程表格优化
- Batch UI-2：流程操作按钮分组
- Batch UI-3：单次服务调试区优化

原因：

- 都集中在“诊断调试与流程测试”Tab。
- 使用频率最高。
- 改动主要是布局和状态显示，不影响底层协议。
- 一次完成后，手动发送和自动化流程体验会明显提升。

重点验证：

- 单次发送。
- 添加到流程。
- 自动化流程执行。
- 期望响应断言。
- 失败策略继续/停止。
- 清空列表确认。

### 第二批：查找与日志优化

合并内容：

- Batch UI-4：服务树搜索与快捷操作
- Batch UI-5：日志过滤与搜索

原因：

- 都属于“查找和排查效率”优化。
- 对协议收发无直接影响。
- 可以共用搜索/过滤的交互思路。

重点验证：

- 服务树搜索为空时显示全部。
- 搜索服务号、名称、PDU。
- 双击模板填入请求。
- 日志 TX/RX/ERR/SYS 过滤。
- 日志关键字搜索。
- 日志导出内容符合预期。

### 第三批：刷写配置区整理

合并内容：

- Batch UI-6：刷写配置区分组优化

原因：

- 刷写区域涉及核心升级入口，建议单独做。
- 只调整布局，不改 `buildUpgradeConfig()` 和升级状态机。
- 便于单独验证默认刷写参数和非法配置拦截。

重点验证：

- 默认配置仍能启动升级。
- 非法 Seed/Key、ALFI、Routine ID 能被拦截。
- 升级期间新增配置控件全部锁定。
- 完成/失败/中止后控件恢复。

## 推荐执行顺序

1. 第一批：诊断操作区优化
2. 第二批：查找与日志优化
3. 第三批：刷写配置区整理

## 每批通用验证

- `git diff --check`
- Qt/MinGW 编译
- 页面启动无崩溃
- 单次发送可用
- 自动化流程可用
- DTC Tab 可用
- 固件升级按钮和配置校验可用
- 清空/删除/导入类操作有容错或确认
