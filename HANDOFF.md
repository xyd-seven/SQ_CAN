# Project Hand Off

## 1. 项目目标

基于周立功 CAN 工具二次开发 Qt 上位机，用于高精度 IMU 实时数据显示、姿态图展示、数据透传、串口控制和日志分析。

## 2. 当前状态

- [x] 已完成：车姿监控 2D 姿态图轴向映射设置，支持 X/Y/Z 正负轴配置并持久化。
- [x] 已完成：轴向映射同步作用于车姿监控、IMU 数据透传、串口控制三个 tab 的 2D 图。
- [x] 已完成：车姿监控解析数据保存为 CSV，时间戳保留毫秒，缺失协议字段填 0。
- [x] 已完成：CAN 接收时间源统一为 ZLG CAN 接收时间戳，并同步到解析 CSV、透传 TXT。
- [x] 已完成：CAN 收发控制页接收时间显示修正，移除临时 DevTime 列。
- [x] 已完成：透传完整包解析改为队列缓存，避免 UI 刷新丢包。
- [x] 已完成：透传分包解析按 0x01~0x0C 固定循环协议处理，0x01 作为新包起点。
- [x] 已完成：程序图标资源接入。
- [x] 已完成：CAN 收发控制页滚动离开底部时冻结表格显示，最大显示行数提升到 1000。
- [x] 已完成：更新 `.gitignore`，忽略本地工具、协议文件、qmake 生成文件和旧库备份。
- [x] 已完成：数据透传 Tab 新增 0xCD44 blk9/10/11 块数据读取指令，且自定义十六进制指令按实际长度分包下发。
- [x] 已完成：实现数据透传 Tab（CAN/CAN FD 模式）下发读取指令后，返回 256 字节数据的 0x01~0x20 拼包与解析展示，同步弹出配置表格（与串口一致）。
- [x] 已完成：在 IMUParser 中防御过滤非 96 字节的首帧（0x01），保障姿态解算对配置块拼包返回的抗干扰性。
- [ ] 未完成：日志保存后台线程化、自动分卷、长期一夜保存稳定性专项优化。
- [ ] 未完成：历史编译 warning 清理。

## 3. 当前任务

当前正在处理的是项目交接文件生成。

涉及模块：

- 项目文档
- Git 工作区状态

涉及文件：

- `Hand Off Rules.md`
- `HANDOFF.md`
- `.gitignore`

## 4. 关键设计决策

- 姿态图轴向配置只影响图形显示，不改变原始 IMU/CAN 数据解析。
- 三个 tab 的 2D 姿态图共用同一套图形坐标设置。
- 解析 CSV、透传 TXT、CAN 收发控制页时间显示统一使用 CAN 接收时间源。
- 透传原始完整包使用队列缓存，UI 定时批量消费，避免高频数据被单个最新包覆盖。
- CAN 收发控制页日志保存独立于 UI 实时显示状态，用户暂停/冻结表格刷新时仍继续写日志。
- CAN 收发控制页表格只保留最近 1000 行，避免 UI 表格无限增长。
- `协议文件/`、`TOOL/`、本地 qmake 生成文件和旧 DLL/H 备份不提交仓库。
- **CAN 模式下的配置块（0xCD44/0xFD44）拼包拦截设计**：设备返回 256 字节的 blk 数据时，通过 CAN 发送占用 `0x01` ~ `0x20` 的 ID 范围，与 96 字节高频原始数据流（`0x01` ~ `0x0C`）发生重合冲突。我们通过在 `0x01` 帧根据 `data[2]` 长度域（`0xFC` 标识块配置包，`0x5C` 标识普通姿态包）进行分类路由：块配置包状态下拦截 `0x01` ~ `0x20` 帧不送入姿态解析器（`IMUParser`），防止仪表盘异常或崩溃；拼齐 32 帧且校验通过后自动弹出与串口一致的解析窗展示数据。

## 5. 修改记录

修改文件：

- `.gitignore`
- `qt-example_32/CAN/CAN.pro`
- `qt-example_32/CAN/main.cpp`
- `qt-example_32/CAN/mainwindow.cpp`
- `qt-example_32/CAN/mainwindow.h`
- `qt-example_32/CAN/imuparser.cpp`
- `qt-example_32/CAN/imuparser.h`
- `qt-example_32/CAN/imuwidgets.cpp`
- `qt-example_32/CAN/imuwidgets.h`
- `qt-example_32/CAN/resources.qrc`

新增文件：

- `qt-example_32/CAN/icons/app.png`
- `qt-example_32/CAN/icons/app.ico`
- `HANDOFF.md`

删除文件：

- UNKNOWN

## 6. 已知问题

P0：

- UNKNOWN

P1：

- 长时间高频日志保存仍在 UI 主线程写文件，保存一夜存在卡顿或异常退出风险。
- 日志文件未做按大小/时间自动分卷，长时间保存后单文件可能过大。

P2：

- 编译仍存在 signed/unsigned comparison warning。
- `onDecodeBinClicked()` 中存在 `readFloat32` set but not used warning。
- 部分旧源码中文注释在 PowerShell `Get-Content` 输出中显示乱码，需避免用错误编码重写大段文件。
- 透传/串口下发参数输入框曾出现 placeholder 与真实 text 混用风险，后续改动需重点复查。

## 7. 下一步任务

1. 实物测试 CAN 收发控制页滚动冻结逻辑：滚动离开底部后表格应稳定，回到底部后恢复刷新。
2. 实物测试一夜日志保存：记录文件增长速度、UI 卡顿情况、磁盘占用和异常提示。
3. 设计并确认后台日志写线程方案：队列容量、丢弃策略、flush 策略、错误上报、停止保存流程。
4. 设计并确认日志自动分卷方案：按大小或按时间分卷，文件命名包含开始时间和序号。
5. 清理编译 warning：优先处理 signed/unsigned comparison 和未使用变量。
6. 复查透传/串口指令下发默认参数：确认默认值是真实 text 或读取空值时有协议默认值。

## 8. 测试状态

- PASS：qmake + mingw32-make 编译通过。
- PASS：CAN 收发控制页滚动冻结修改已编译通过。
- PASS：`.gitignore` 更新后，未跟踪本地工具/协议文件不再显示在 Git status 中。
- PASS：数据透传配置块读取指令添加、指令动态长度分包下发、CAN 模式下返回包的 32 帧拼包校验和弹窗解析功能，通过 Release 模式完整编译。
- UNKNOWN：2D 姿态图轴向设置在最新实物条件下的完整回归结果。
- UNKNOWN：透传完整包队列缓存修改后的长时间运行稳定性。
- UNKNOWN：CAN/CSV/TXT 日志保存一夜稳定性。
- UNKNOWN：透传/串口指令下发在当前硬件程序版本下是否全部生效。

## 9. 对下一个 Agent 的要求

- 先阅读相关实现，再给结论或修改方案。
- 不扫描整个项目。
- 非必要不读取大文件。
- 保持现有架构。
- 保持现有代码风格。
- 修改前分析影响范围。
- 遵守《AI Agent 工作准则》。
- 大于 3 个文件、超过 300 行、核心逻辑或架构相关改动，必须先输出 Markdown 方案并获得用户确认。
- 不要擅自删除本地文件、协议文件、工具目录或用户未提交改动。
- 注意本项目源码存在编码历史问题，编辑时避免大范围重写文件。
- 发现以下情况立即停止并询问用户：
  - 需求不明确
  - 涉及数据库结构调整
  - 涉及接口协议变更
  - 涉及跨模块重构
  - 涉及架构调整
  - 无法确认影响范围
